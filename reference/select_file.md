# Interactively select from options

Interactively select from options

## Usage

``` r
select_file(fnames)
```

## Arguments

- fnames:

  File names to select from

## Examples

``` r
# fnames = c("f1", "f2")
# stats19:::select_file(fnames)
```
